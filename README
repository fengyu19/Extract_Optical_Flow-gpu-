I changed the original source code for own dataset.

Requirements:
opencv2.4
cuda8.0

Usage:
mkdir build
cd build
cmake -D CUDA_USE_STATIC_RUNTIME=OFF ..
make
./denseFlow_gpu -f vidFile -start x -end y -b bound -d device_id -s segment_number

vidFile: folder contains all the videos
segment_number: the number of segments proposed to get
